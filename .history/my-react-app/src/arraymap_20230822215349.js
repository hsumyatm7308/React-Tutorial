import React from "react";
import { ReactDOM } from "react";

const myarray = ["apple","orange","grapes"];

const arrmap = myarray.map(()=>{
        
});

const container = document.getElementById('root');
const root = ReactDOM.createRoot(container);
root.render();